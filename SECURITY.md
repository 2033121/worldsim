# 安全政策

## 报告漏洞

如果发现安全问题（尤其是密钥泄漏、任意代码执行、数据泄露），**请不要开公开 Issue**，直接邮件联系或私信。

- **联系渠道**：GitHub Issues 私密选项 / 仓库 Discussions / 直接 PR
- **响应时间**：72 小时内确认，7 天内修复或给出缓解方案

## 已知安全边界

| 项目 | 说明 |
|---|---|
| **api.json** | 含 LLM API Key，只存在于本地运行目录，**永不入库**（.gitignore 已排除） |
| **本地服务** | 48091/48090 端口仅绑定 127.0.0.1（默认），局域网访问需手动改配置 |
| **素材库** | `material/` 为风格参考（网文提取），不入库；商用前请自行确认版权 |
| **MCP Server** | 仅桥接本机 API，不暴露密钥给客户端 |

## 安全提交检查清单

- [ ] 没有提交 `api.json` / `*.key` / `*.pem` / `.env` / token
- [ ] 日志里没有打印完整密钥
- [ ] 插件包分发 zip 里只有 `api.json.example`

## 依赖

本项目 Go 与 Python 均**零第三方依赖**（纯标准库），供应链风险最小化。GitHub Actions 由 Dependabot 每周自动检查版本。